server:
  port: 8080
<<<<<<< HEAD

spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:h2:mem:ai_learning_dev;DB_CLOSE_DELAY=-1}
    username: ${JDBC_DATABASE_USERNAME:sa}
    password: ${JDBC_DATABASE_PASSWORD:}
    driver-class-name: ${DB_DRIVER:org.h2.Driver}
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

# Cấu hình AI provider (tích hợp sau)
ai:
  provider: openai
  api-key: ${OPENAI_API_KEY:}

# Google OAuth2
google:
  client-id: ${GOOGLE_CLIENT_ID:}
  client-secret: ${GOOGLE_CLIENT_SECRET:}
=======
spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
ai:
  provider: openai
  apiKey: ${OPENAI_API_KEY:}
>>>>>>> origin/develop
