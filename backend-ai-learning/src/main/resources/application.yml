server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
ai:
  provider: openai
  apiKey: ${OPENAI_API_KEY:}
